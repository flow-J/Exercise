#include <iostream>

using std::cin;
using std::cout;
using std::endl;

int a, b, c, d;

int main()
{
    while (cin >> a >> b >> c >> d)
        if (a > b && b > c && c >> d)
            cout << "a > b > c > d" << endl;
}
