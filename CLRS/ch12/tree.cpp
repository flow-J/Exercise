#include <iostream>

struct node
{
    key;
    left;
    right;
};

class tree
{
public:
    tree_walk(val)
    {
        if (val != nullptr)
        {
            inorder(val,left);
            cout << val.key << endl;
            inorder(val,right);
        }
    }

    inorder()
    {}

    search(tree, k)
    {
        if (tree == nullptr || k == tree.key)
        {
            return tree;
        }
        if (k < tree)
    }

private:
        
};

inline operator <<(ostream& os, val)
{
    std::cout << val.
}
